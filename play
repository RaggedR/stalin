#!/bin/sh
# play — start the commissariats and open an interactive plan.
set -e
cd "$(dirname "$0")"
./up.sh "$@" >/dev/null
trap './down.sh >/dev/null 2>&1 || true' EXIT INT TERM
[ "$1" = "--agent" ] && shift
deno run --quiet --allow-net --allow-env play.ts "$@"
